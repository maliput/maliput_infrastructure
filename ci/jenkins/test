#!/bin/bash

set -eo pipefail

source ./.bashrc

source ./install/setup.sh

colcon test --event-handlers=console_direct+ --return-code-on-test-failure \
       --packages-skip PROJ4 ${COLCON_TEST_EXTRA_ARGS:-}
